```dataviewjs
dv.header(2, "🎬 SEASON DIRECTOR"); const scenes = dv.pages('"01_NODES/scenes"') .where(s => s.type === "scene" && s.sequence != null) .sort(s => s.sequence) .array(); if (!scenes.length) { dv.paragraph("No sequenced scenes."); return; } const arcSize = 4; for (let i = 0; i < scenes.length; i += arcSize) { const arc = scenes.slice(i, i + arcSize); const avg = arc.reduce((sum, s) => sum + (s.continuity_score ?? 0.5), 0) / arc.length; dv.header(3, `ARC ${Math.floor(i/arcSize)+1} (${arc.length} scenes)`); dv.paragraph(`Avg Continuity: ${avg.toFixed(2)} — ${avg >= 0.6 ? "✅ Strong" : "⚠️ Weak"}`); dv.list(arc.map(s => s.file.link)); }