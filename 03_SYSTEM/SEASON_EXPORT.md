```dataviewjs
 dv.header(2, "📦 SEASON EXPORT PACKAGE"); const scenes = dv.pages('"01_NODES/scenes"') .where(s => s.type === "scene" && s.sequence != null) .sort(s => s.sequence) .array(); let time = 0; let output = []; scenes.forEach(s => { const dur = s.duration ?? 4; output.push({ seq: s.sequence, scene: s.file.name, start: time.toFixed(1), end: (time + dur).toFixed(1), duration: dur, objective: s.objective || "" }); time += dur; }); dv.table(["Seq", "Scene", "Start", "End", "Dur", "Objective"], output.map(o => [o.seq, o.scene, o.start, o.end, o.duration, o.objective])); dv.header(3, "JSON Export"); dv.code(JSON.stringify(output, null, 2), "json");