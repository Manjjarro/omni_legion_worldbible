```dataviewjs
dv.header(2, "🖼️ STORYBOARD MATRIX"); const scenes = dv.pages('"01_NODES/scenes"') .where(s => s.type === "scene") .sort(s => s.sequence) .array(); let planned = 0, generated = 0, failed = 0; scenes.forEach(s => { if (s.storyboard_status === "Generated") generated++; else if (s.storyboard_status === "Failed") failed++; else planned++; }); dv.paragraph(`**Pipeline:** Planned: ${planned} | Generated: ${generated} | Failed: ${failed}`); let rows = scenes.map(s => { let imageRender = "❌ No Asset"; if (s.preview_image && s.preview_image.trim() !== "") { imageRender = `![[${s.preview_image.trim()}|100]]`; } return [ s.sequence ?? "??", s.file.link, imageRender, `${s.shot_type || "—"} / ${s.motion_preset || "Static"}`, (s.objective || "").substring(0, 45) + "...", s.storyboard_status || "Planned" ]; }); dv.table(["Seq", "Scene", "Preview", "Shot/Motion", "Objective Summary", "Status"], rows);