```dataviewjs
dv.header(2, "📐 EPISODIC PACING ANALYSIS"); const scenes = dv.pages('"01_NODES/scenes"') .where(s => s.type === "scene" && s.act != null) .sort(s => s.sequence) .array(); if (!scenes.length) { dv.paragraph("Assign `act:` numbers in scene frontmatter."); return; } [1,2,3].forEach(actNum => { const actScenes = scenes.filter(s => s.act === actNum); if (!actScenes.length) return; const totalDur = actScenes.reduce((sum, s) => sum + (s.duration ?? 4), 0); const avgCont = actScenes.reduce((sum, s) => sum + (s.continuity_score ?? 0.5), 0) / actScenes.length; dv.header(3, `ACT ${actNum} (${actScenes.length} scenes)`); dv.paragraph(`Duration: ${totalDur}s | Avg Continuity: ${(avgCont*100).toFixed(0)}%`); dv.list(actScenes.map(s => s.file.link)); });